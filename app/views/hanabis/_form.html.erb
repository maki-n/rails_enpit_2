<%= form_for(@hanabi) do |f| %>
  <% if @hanabi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hanabi.errors.count, "error") %> prohibited this hanabi from being saved:</h2>

      <ul>
      <% @hanabi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_field :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
