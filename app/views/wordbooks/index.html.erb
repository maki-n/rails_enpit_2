<p id="notice"><%= notice %></p>

<h1>グループ０</h1>

<h2>英単語の学習</h2>

<hr>
<p><%= link_to '英単語テスト', "/wordbooks/quiz" %></p> 
<hr>

<P>検索する（英語を入力してください）
<%= form_tag wordbooks_path, :method => 'get' do %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "検索", :english => nil %>
<% end %>
</p>
<p><%= link_to 'すべての登録単語を表示する', wordbooks_path %></p>

<hr>
<h3>単語一覧</h3>
<table>
  <thead>
    <tr>
      <th>日本語</th>
      <th>英語</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @wordbooks.each do |wordbook| %>
      <tr>
        <td><%= wordbook.japanese %></td>
        <td><%= wordbook.english %></td>
        <td><%= link_to '確認', wordbook %></td>
        <td><%= link_to '修正', edit_wordbook_path(wordbook) %></td>
        <td><%= link_to '削除', wordbook, method: :delete, data: { confirm: '削除しますか' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新規単語登録', new_wordbook_path %>

<hr>

<p><%= link_to 'グループ０目次に戻る', root_path %></p>
